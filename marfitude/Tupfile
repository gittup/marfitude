include_rules
CFLAGS += `sdl-config --cflags`
: foreach *.c |> !cc |> {objs}

libs += $(MARFITUDE_ROOT)/gmae/libgmae.a
libs += $(MARFITUDE_ROOT)/util/libutil.a
LDFLAGS += `sdl-config --libs`
LDFLAGS += -lGL
LDFLAGS += -lSDL_image
: {objs} objs/*.o $(libs) |> !ld |> marfitude
