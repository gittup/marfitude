include_rules
CFLAGS += `pkg-config sdl --cflags`
CFLAGS += -I..
CFLAGS += -fpic
: foreach *.c |> !cc |> {objs}

LDFLAGS += `pkg-config sdl --libs`
LDFLAGS += $(GL_LDFLAGS)
LDFLAGS += -lSDL_image
LDFLAGS += $(PLUGIN_LDFLAGS)
: {objs} $(libs) |> !ld |> space.so
