include_rules
CFLAGS += `pkg-config sdl --cflags`
CFLAGS += -I..
CFLAGS += -fpic
: foreach *.c |> !cc |> {objs}

LDFLAGS += `pkg-config sdl --libs`
LDFLAGS += -lGL
LDFLAGS += -lSDL_image
LDFLAGS += -shared
: {objs} $(libs) |> !ld |> space.so
